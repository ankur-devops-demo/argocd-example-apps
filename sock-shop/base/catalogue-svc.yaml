---
apiVersion: v1
kind: Service
metadata:
  name: catalogue
  labels:
    name: catalogue
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 80
  selector:
    name: catalogue
